import os.internal;
decl uint ;
decl fnc exec(command string, args []string);

@MainFunc
fnc exec(command string, args []string): os.internal {
    if command.size < 1 {
        self.out.put("Wooot");
    }
}
